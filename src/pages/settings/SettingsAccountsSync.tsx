import { SettingsScreen } from './_shared/SettingsScreen';

export function SettingsAccountsSync() {
  return (
    <SettingsScreen title="ACCOUNTS & SYNC">
      <p className="px-4 pb-8 font-mono text-[12px] text-textLo">
        Placeholder — connected banks, sync schedule, pending jobs land in 10c.
      </p>
    </SettingsScreen>
  );
}
