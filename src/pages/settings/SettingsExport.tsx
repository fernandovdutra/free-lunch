import { SettingsScreen } from './_shared/SettingsScreen';

export function SettingsExport() {
  return (
    <SettingsScreen title="EXPORT">
      <p className="px-4 pb-8 font-mono text-[12px] text-textLo">
        Placeholder — data export + ICS import land in 10f.
      </p>
    </SettingsScreen>
  );
}
